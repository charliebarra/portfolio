# Custom domain update

Public canonical domain: `https://www.charliebarra.com`

GitHub Pages deployment source: `charliebarra/portfolio`

## Modified files

- `index.html` — added canonical URL, complete Open Graph and social-share metadata, absolute share image, and Person/WebSite structured data.
- `story.html` — added canonical URL and complete Open Graph and social-share metadata.
- `projects.html` — added canonical URL and complete Open Graph and social-share metadata.
- `meteor-mayhem.html` — added canonical URL and complete Open Graph and social-share metadata.
- `programming.html` — added canonical URL and complete Open Graph and social-share metadata.
- `world-design.html` — added canonical URL and complete Open Graph and social-share metadata.
- `pokemon.html` — added canonical URL and complete Open Graph and social-share metadata.
- `notebook.html` — added canonical URL and complete Open Graph and social-share metadata.
- `resume.html` — added canonical URL and complete Open Graph and social-share metadata.
- `contact.html` — added canonical URL and complete Open Graph and social-share metadata.
- `about.html` — changed the canonical URL to the absolute Story URL and marked the legacy redirect as `noindex, follow`.
- `python-game.html` — changed the canonical URL to the absolute Work URL and marked the legacy redirect as `noindex, follow`.
- `probability-simulator.html` — changed the canonical URL to the absolute Work URL and marked the legacy redirect as `noindex, follow`.
- `learning-sandbox.html` — changed the canonical URL to the absolute Work URL and marked the legacy redirect as `noindex, follow`.
- `README.md` — replaced public-facing GitHub Pages URLs with `www.charliebarra.com` and clarified that deployment still runs through GitHub Pages.
- `site.webmanifest` — set root `id`, `start_url`, and `scope` for the custom domain.
- `FILE-INVENTORY.md` — documented the new domain and search-engine configuration files.

## Added files

- `CNAME` — assigns `www.charliebarra.com` to this GitHub Pages repository.
- `robots.txt` — permits indexing and identifies the canonical sitemap.
- `sitemap.xml` — lists all primary public pages at the canonical domain.
- `CUSTOM-DOMAIN-CHANGELOG.md` — records this update.

## Preserved links

- Internal navigation remains relative so it works on both the custom domain and the underlying GitHub Pages deployment.
- GitHub profile and repository links still point to `github.com/charliebarra`.
- YouTube embeds and channel links are unchanged.
- The résumé remains linked from `documents/Charlie-Barra-Resume.pdf`.
